"""
inference.py — the detective: guess which of the four hidden causes applies.

Two arms, rules first then a depth-4 decision tree, both scored on a held-out
split with a 4x4 confusion matrix.

PHASE 3. Not built yet.
"""

from __future__ import annotations


def infer(*args, **kwargs):
    raise NotImplementedError("inference.infer is Phase 3. Finish Gates 1-2 first.")
