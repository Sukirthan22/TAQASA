"""
harness.py — the scoreboard. Runs any policy over all 500 invoices and scores it.

PHASE 2. Not built yet — the build order is deliberate: the measuring rig comes
before the thing being measured, so the agent can never be tuned against a
scoreboard that was written to flatter it.
"""

from __future__ import annotations


def run_policy(*args, **kwargs):
    raise NotImplementedError("harness.run_policy is Phase 2. Finish Gate 1 first.")
